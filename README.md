# SFGPersonalProjects
Version controlling some summary projects I've been working on!

## Modules
To run Python modules, configure desired functions in `main.py` and run from the terminal accordingly

## Dashboards
To run dashboards, enter `streamlit run dashboards/{DASHBOARD_NAME}.py` in the terminal

## Utils
Currently just contains config constants across functions